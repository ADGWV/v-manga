/├── index.html          ← الرئيسية (بحث + شريط سفلي + عرض المستخدم)├── login.html          ← دخول / حساب جديد / جوجل├── profile.html        ← البروفايل├── settings.html       ← الإعدادات (اسم + صورة + باسورد)├── manga.html          ← التفاصيل + الفصول + المفضلة├── reader.html         ← القارئ├── admin.html          ← لوحة الأدمن (إضافة مانجا/فصل + ZIP)├── css/│   └── style.css       ← التصميم الجديد (سماوي + بنفسجي)└── js/    ├── firebase.js     ← Firebase + حفظ الدخول + الرفع    └── app.js          ← المنطق + البحث + عرض المستخدم